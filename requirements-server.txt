# 웹 서비스(backend/server.py) 런타임 전용 의존성 — Docker 이미지용.
# legacy 실험 도구(lancedb/tantivy/unstructured/pypdf 등)는 서버 경로 미사용이라 제외.
fastapi
uvicorn[standard]
agno
openai
fastembed
rank-bm25
networkx
numpy
requests
python-dotenv
PyYAML          # 사용자·역할 정의(users.yaml)
PyJWT[crypto]   # OIDC id_token 검증(SSO)
mcp>=2.0        # MCP 서버(/mcp streamable-HTTP + stdio)
httpx           # MCP 도구의 백엔드 호출(인프로세스 ASGI 포함)
